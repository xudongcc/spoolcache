"""Repository-owned SpoolCache qualification tools.

Keeping this directory an explicit package prevents an unrelated site-package
named ``benchmarks`` from shadowing these helpers in target runtime images.
"""
